type: object
additionalProperties: false
required:
  - id
  - name
properties:
  id:
    type: string
    pattern: "^[a-z0-9-]+$"
  name:
    type: string
  place:
    type: string
  status:
    type: string
    enum:
      - active
      - retired